=============================================================================
 FACTORY LOOP — One prompt, zero fill-in, works on any repo
=============================================================================
 How to use:
   1. Tell Claude: "Pull up ~/repos/<PROJECT>"   (or paste the full path)
   2. Copy the prompt below (between the ===== lines)
   3. Paste. Send. Walk away.

 The prompt auto-detects: new-vs-existing, stack, current version, themes,
 scope guards, iteration count, and release target. Nothing to fill in.

 CANONICAL ROUTING (default) — copilot-heavy:
   The default preset is now copilot-heavy so Claude Max quota is preserved
   for genuine escalation only. Bulk research, synthesis, implementation,
   counter-passes, UX, theming, and audit all route through Copilot's
   Sonnet 4.6 and GPT-5.3-Codex. Gemini Flash seeds broad research. Direct
   Claude Max fires only when the work genuinely needs Opus-level reasoning
   (PEC rubric UNCERTAIN ≥3 rounds, debate stalemate, security escalation).

   Swap routing: ~/.claude-octopus/bin/octo-route.sh <mode>
   Modes: copilot-heavy (default) | balanced | claude-heavy | codex-heavy |
          direct-only | copilot-only

 Design principle of this prompt:
   The RECIPE is the source of truth. This prompt does not restate recipe
   internals — doing that is what caused drift in earlier versions. Updates
   to the flow (new phases, new gates, new directives) belong in
   recipe-factory-loop.md, not here. This prompt handles only the dynamic
   bits (path, goal, iteration count), the offload policy, the research
   expectation, and a minimal hard-cap/never backstop.

=============================================================================
 THE ONLY PROMPT YOU NEED — copy everything between the lines below
=============================================================================

Run the factory loop on the repo path I just gave you. Autonomous mode —
decide and proceed, no clarifying questions. Do substantial work. "Extensive"
is the default setting, not the opt-in.

Follow recipe-factory-loop.md in memory EXACTLY, end-to-end. The recipe is
the source of truth; anything restated below is backstop only, not a
substitute for reading the recipe.

PRE-FLIGHT (run before anything else):
- Invoke `bash ~/repos/octopus-factory/bin/factory-doctor.sh`. Surface the
  output to me ONCE before kicking off. Hard failures (exit 1) halt the run
  with a diagnostic — fix and re-invoke. Soft warnings (exit 2) proceed but
  acknowledge them in the kickoff message so I know what won't fire.
- The doctor specifically catches the failure mode where the active preset
  routes audit phases to `copilot-codex` (Copilot's GPT-5.3-Codex) instead
  of the standalone `codex` CLI — historically my factory runs ran
  Claude-only in single-session because that preset routing never got
  exercised. v0.5.1+ fixes this via codex-direct.sh; the doctor verifies it.

CODEX DISPATCH (single-session and orchestrated alike, v0.5.1+):
- Audit phases (L3 Critic, U1 UX, T1 theming, Q1 security, Q2 review,
  roadmap-research Phase 5 self-audit) MUST shell out to
  `bash ~/repos/octopus-factory/bin/codex-direct.sh <phase>` for real
  cross-family signal. Wrapper handles model selection, sandbox,
  transcript capture, auth/quota error classification.
- This works regardless of the active preset and regardless of whether
  orchestrate.sh is healthy — it bypasses both. The codex CLI on PATH +
  ~/.codex/auth.json with a valid plan is the only requirement.
- If codex-direct.sh returns non-zero (exit 2 auth, 3 quota, 4 timeout,
  5 refusal, 6 internal): log the degradation ("L3 audit fell back to
  Claude-only because codex-direct returned exit=N"), continue the run
  with the master Claude doing the audit instead. Never halt the whole
  factory run on a single audit-phase failure.
- This restores the cross-family audit signal that was silently absent
  from every single-session run before v0.5.1.

OFFLOAD POLICY (non-negotiable):
- The canonical routing preset is `copilot-heavy`. Verify that preset is
  active via `cat ~/.claude-octopus/config/providers.json | grep _mode`. If
  the active preset is anything else, swap to copilot-heavy for this run
  via `~/.claude-octopus/bin/octo-route.sh copilot-heavy` before starting
  (unless I explicitly overrode it this session).
- Route the HEAVY LIFT through Copilot. That means: broad research synthesis,
  ROADMAP replenish, per-task implementation, counter-passes, UX/theming
  first-pass AND counter-pass, audit first-pass. The master Claude session
  (this session) dispatches, coordinates, and reviews — it does NOT handle
  bulk implementation or routine audits on its own.
- Claude Max (this session) ONLY fires as itself when:
    1. A PEC rubric returned UNCERTAIN on ≥3 rounds on the same task (Opus
       escalation). OR
    2. A three-role debate stalemates (KS distance never drops below 0.05
       across max rounds). OR
    3. A security-critical finding needs Opus-level reasoning to resolve. OR
    4. Architectural novelty on a NEW project's foundational decisions
       where Sonnet 4.6 via Copilot would be undershooting.
  If none of (1-4) are happening, route work to Copilot. Catching yourself
  doing bulk implementation in this Claude session without an escalation
  reason is a routing bug — stop and delegate.
- Gemini Flash seeds broad research (the 9-dimension landscape scan). Direct
  ChatGPT Pro Codex stays cold unless `--final-codex-pass` is set.

RESEARCH EXPECTATION (default is "extensive", not "quick"):
- L1 applies `directive-roadmap-research.md` — the five-phase research
  protocol (Phase 0 repo recon → Phase 1 external → Phase 2 quantity-first
  feature harvesting → Phase 3 six-dimension scoring + five-tier bucketing
  → Phase 4 author/reconcile ROADMAP.md → Phase 5 adversarial self-audit).
  Runs ALWAYS on existing repos regardless of ROADMAP state — research
  EXPANDS it, never bypasses when "full".
- Phase 1 has a 30-60 distinct-source FLOOR with every URL recorded in an
  Appendix. Phase 2 extracts 80-200+ raw items before any filtering.
  Phase 3 scores each on Fit / Impact / Effort / Risk / Dependencies /
  Novelty and assigns to Now / Next / Later / Under Consideration /
  Rejected. Rejected items retain reasoning so future runs can't silently
  resurrect them.
- Phase 5 self-audit runs on a DIFFERENT model family than Phases 2-4
  (copilot-codex reviews copilot-sonnet's work) and checks seven dimensions:
  source traceability, tier placement reasoning, category coverage
  (security/a11y/i18n/observability/testing/docs/distribution/plugin/
  mobile/offline/multi-user/migration/upgrade), internal consistency,
  adversarial review, charter alignment, file-on-disk.
- Research artifacts land in `docs/research/iter-<N>-*.md`:
    state-of-repo.md (Phase 0)
    sources.md + landscape.md (Phase 1)
    harvest.md (Phase 2, 80-200+ raw items)
    scored.md (Phase 3, tiered + full scoring blocks)
    audit.md (Phase 5, 7-check result)
- The 9 source classes (extension of the old 9 dimensions):
    1. Direct OSS competitors (10-25 projects)
    2. Commercial / closed-source competitors
    3. Adjacent-domain projects (steal architecture/UX/plugin patterns)
    4. Awesome-lists
    5. Community signal (Reddit/HN/Lobsters/SO/forums/Discord)
    6. Standards / specs / RFCs / platform APIs
    7. Academic + engineering blogs + conference talks
    8. Dependency changelogs (features you could expose but haven't)
    9. Security advisories + CVE databases
- Iteration 2+ is DELTA mode on Phase 1 only (net-new since prior
  iter-<N-1>-sources.md). Phases 2-5 still run in full but merge-into
  rather than rebuild-from-scratch.

Lazy-load directives only when the phase that needs them fires:
  directive-roadmap-research.md  (L1 — five-phase research: recon → external
                                  → harvest → score → reconcile → self-audit)
  directive-audit.md             (L3/L4 fallback)
  directive-debate.md            (L3/L4 primary — three-role Grader/Critic/Defender)
  directive-circuit-breakers.md  (runs alongside every phase)
  directive-modularization.md    (M-phase — decompose monoliths, behavior-preserving)
  directive-ux-polish.md         (U1/U2 — GUI polish, interactive states, motion)
  directive-theming.md           (T1/T2 — all-theme interactive-state coverage)
  directive-dependency-scan.md   (D1/D2 — CVE scan + fix)
  directive-secret-scan.md       (every commit gate)
  directive-logo.md              (P5 for new projects, G-phase for existing)
  recipe-ai-scrub.md             (S-phase on existing repos)
  recipe-release-build.md        (Q3 — project-type-aware build + sign + release)

AUTO-DETECT:
- No .git directory → NEW project. Run full preflight (scaffold + OSS research
  + logo + gh repo create --private + branch protection). S-phase auto-skips
  (no prior history to scrub). W-phase auto-skips (no WIP yet).
- Has .git → EXISTING project. Pass --skip-preflight. W-phase auto-runs if
  uncommitted work present. S-phase auto-runs early to scrub AI-reference
  history (autopilot: backup + apply + push; downgrades to manual on
  multi-contributor or GPG-signed commits). G-phase auto-runs if icon set
  missing (SVG-via-Copilot primary, no OpenAI billing needed).
- Pick the best stack for the problem on NEW projects (per "Best tool for the
  job" rule in ~/.claude/CLAUDE.md — do not default to Python or PowerShell).
  Load the matching stack-*.md after the decision.
- On EXISTING projects: detect stack from build files (package.json / Cargo.toml
  / *.csproj / build.gradle / go.mod / pyproject.toml / etc.). Load the matching
  stack-*.md.
- Read repo CLAUDE.md + linked memory file + ROADMAP.md + rtk git log -20 before
  deciding the goal on EXISTING projects.
- Derive the goal from repo state AFTER running L1 research (research may
  promote a different top priority than the current ROADMAP suggests):
    ROADMAP has unchecked items + research surfaces high-impact additions
      → advance ROADMAP with research additions prioritized
    Pending release → ship it AFTER L3/L4 audit closes cleanly
    Open audit findings → close them, then replenish + advance
    Empty / clean → hardening + polish pass, still run full L1 research to
      seed the next ROADMAP round
- Derive iteration count (higher than prior defaults — default is heavy):
    NEW = 5 iterations
    EXISTING with active roadmap = 4 iterations
    EXISTING clean (no roadmap, no findings) = 2 iterations (research + one
      implementation cycle minimum — don't ship "nothing changed" runs)
  Stop-on-convergence and stop-on-regression fire regardless of the count.
- Next version: current patch bump unless roadmap work is major/breaking (minor
  bump). Detect current via rtk grep on manifest/README badge. The single bump
  happens at Q3, NOT per iteration.
- Scope guards: extract from repo CLAUDE.md + memory file charter language.
  Reject ROADMAP items that violate the stated charter (tag as CHARTER-REVIEW
  and defer — do NOT silently drop, the user may want to expand charter).

EXECUTE:
  /octo:factory <THE_PATH_I_GAVE_YOU> --iterations <DETECTED_N> \
      [--skip-preflight] [--audit-only] [--skip-scrub] [--manual-scrub] \
      [--skip-wip-adoption] [--skip-logo] [--force-logo] [--raster-logo] \
      [--single-session] [--require-orchestrator] \
      [--task <id>] [--plan] [--final-codex-pass]

EXECUTION MODE:
  On entry, detect whether the multi-provider orchestrator is available:
  - orchestrate.sh accessible AND providers.json configured AND Codex/Gemini/
    Copilot CLIs authenticated → ORCHESTRATED mode (recipe runs as designed,
    parallel agents, real three-role debate, full Gemini/Codex/Copilot fan-out).
  - Any prerequisite missing → SINGLE-SESSION mode (one Claude context runs
    every phase serially with declared degradations: L4/U2/T2 collapsed into
    L3/U1/T1, debate falls back to rubric check, image gen still works via
    Copilot-SVG path). Honest fallback, not silent degradation.

  In single-session mode, also check the scale gate:
  - >50K LOC OR >500 files OR >1000 tests OR >30 open ROADMAP items
    → AUTO-ENGAGE LARGE-REPO MODE. Recipe self-modifies and continues:
        - 1 iteration this run (not 3-5)
        - 3 P0/P1 tasks max this run (not 10)
        - Per-task atomic commit + push (not batched at L7)
        - U/T/D phases ROTATED (only run if cadence schedule says due)
        - Q3 release runs only on minor/major bumps OR --release flag
        - Anchored summarization compaction at 60% context (aggressive)
        - State persisted to .factory/large-repo-state.yaml
        - Next run on same repo picks up from last_run state
        - N invocations incrementally drain the ROADMAP — finite + productive
          per run, no halt-mid-session forks
    Override with --halt-on-scale to revert to old halt-and-ask behavior.

  Log the active mode + degradations + Large-Repo-Mode adjustments to
  .factory/state.yaml + .factory/large-repo-state.yaml and the continuation
  brief.

HARD CAPS (backstop — the recipe has the full list):
- L1 ROADMAP replenish: up to 10 NEW P0/P1 tasks per iteration
- L2 implementation: top 10 unchecked P0/P1 items per iteration
- Total iterations: 7 absolute maximum even if detection says otherwise
- Cost: OCTOPUS_FACTORY_MAX_SPEND (default $5) — halt on cap
- Per-agent budget caps enforced by directive-circuit-breakers.md

NEVER (backstop — the recipe has the full list):
- Ask me to confirm scope, version, stack, theme, or iteration count — decide
  and proceed based on repo state
- Burn Claude Max quota on bulk implementation when Copilot Sonnet 4.6 can
  do it — that's a routing bug, offload it
- Skip L1 research on existing projects just because the ROADMAP has items —
  research ALWAYS runs and MAY replenish the ROADMAP with higher-leverage work
- Skip the Definition of Done checklist in ~/.claude/CLAUDE.md before declaring
  anything done
- Commit CLAUDE.md, CODEX_CHANGELOG.md, .claude/, or any AI-reference files
  (the commit gate + secret scan enforce this, but state it explicitly)
- Use --no-verify, --no-gpg-sign, or bypass any hook
- Skip the W-phase WIP-adoption when uncommitted work exists. The factory
  takes ownership: classifies WIP into sensitive/sacred-cow/lockfile/docs/
  config/test/code buckets, runs commit gates per group, commits each clean
  group with role-based messages, pushes, and quarantines anything that fails
  a gate. The legacy "stash first, restore after" behavior is gone — adoption
  is the new default, halt-on-dirty is opt-in via --skip-wip-adoption.
  Mid-merge / mid-rebase / mid-cherry-pick states still halt the run for
  human resolution.
- Run destructive ops (force-push main, reset --hard, drop tables, delete a
  release without rollback) without stopping

If you hit a genuinely ambiguous fork (mutually-exclusive paths, missing
credentials, destructive branch point with no safe default): stop and ask once.
Otherwise proceed.

Begin.

=============================================================================
 OPTIONAL: AUDIT-ONLY VARIANT (no new features, harden + release existing code)
=============================================================================

Run the factory loop in audit-only mode on the repo path I just gave you.
Follow recipe-factory-loop.md. No clarifying questions. Route audit work
through Copilot GPT-5.3-Codex (copilot-heavy preset) — Claude Max escalates
only on genuine ambiguity.

Mode semantics (from the recipe): --audit-only skips P* (preflight), G-phase
(logo), L1/L2 (roadmap + implementation), U* (UX polish), T* (theming).
Runs S* (auto-scrub), L3/L4 (three-role audit debate), L5 (doc drift sync),
L7 (commit gate), D* (CVE/dep scan), and Q* (security → review → SLSA release
with rollback + artifact smoke test).

  /octo:factory <THE_PATH_I_GAVE_YOU> --iterations 1 --audit-only

Begin.

=============================================================================
 OPTIONAL: DRY-RUN / PLAN MODE (preview without executing)
=============================================================================

Generate the factory plan + cost estimate + expected commit count for the repo
path I just gave you. Do NOT execute anything. Follow recipe-factory-loop.md's
--plan mode behavior. Include the projected Copilot Premium Request count
(research + implementation + audit) alongside the USD estimate so I can
judge quota impact before kicking off.

  /octo:factory <THE_PATH_I_GAVE_YOU> --iterations <DETECTED_N> --plan

Begin.

=============================================================================
 OPTIONAL: FINAL-CODEX-PASS VARIANT (release-day high-signal audit)
=============================================================================

On the final iteration before a release, run a direct-ChatGPT-Pro-Codex audit
pass in addition to the Copilot audit. Catches things Copilot's gpt-5.3-codex
can miss. Use this on release day, not on routine runs — burns ChatGPT Pro
quota.

  /octo:factory <THE_PATH_I_GAVE_YOU> --iterations <DETECTED_N> --final-codex-pass

Begin.

=============================================================================
 END OF FILE
=============================================================================
